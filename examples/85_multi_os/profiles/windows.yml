extends:
  - base

target: "$HOMEPATH"

dotfiles:
  # `shared/root` will be deployed by `base`
  - path: "shared\\nvim"
    target: "AppData\\Local\\nvim"
    template: true
  - path: "windows"
    target: "."
